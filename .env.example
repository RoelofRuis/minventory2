# Server Configuration
PORT=3000
SESSION_SECRET=session-secret
JWT_SECRET=supersecret

# Database Configuration
# Options: 'postgres' to use PostgreSQL (requires connection string)
# Or leave empty/set to 'memory' to use an in-memory database (data will be lost on restart).
DB_TYPE=memory

# PostgreSQL Connection String (only used if DB_TYPE=postgres)
# Format: postgres://username:password@host:port/database
DATABASE_URL=postgres://user:password@localhost:5432/minventorydb

# Default Database credentials (if using docker-compose.yml)
# POSTGRES_USER: user
# POSTGRES_PASSWORD: password
# POSTGRES_DB: minventorydb
